[phases.build]
cmds = [
  "npm ci",
  "npx prisma generate",
  "npm run build"
]

[phases.start]
cmds = [
  "npx prisma migrate deploy",
  "npm start"
]

[start]
cmd = "npm start"

[variables]
NODE_ENV = "production"
PORT = "3000"
